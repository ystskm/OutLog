{
  "name": "@synquery/outlog",
  "version": "1.0.2",
  "description": "Simple standard out format interface",
  "main": "index.js",
  "scripts": {
    "jest": "npx jest --clearCache && npx jest --verbose=false",
    "jest-c": "npx jest --clearCache && npx jest --coverage",
    "build": "node build.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^30.2.0",
    "jest-junit": "^16.0.0",
    "ms": "git+https://github.com/ystskm/ms.git"
  }
}
